// script.js

// Utilizar jQuery para detectar cuándo la página se ha cargado completamente
$(document).ready(function() {
    // Este código se ejecutará cuando la página se haya cargado completamente

    // Ejemplo de mostrar un mensaje en una ventana emergente (alert) cuando se haga clic en un botón
    $('#myButton').click(function() {
        alert('¡Hola desde jQuery!');
    });
});
